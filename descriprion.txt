0. Я, видимо, тупой, но я очень устал бодаться с sql-server'ом (при этом не то, чтобы я много времени этому уделил)
Короче, я хотел поставить gui - у меня не получалось
Решил пройтись по еще одному туториолу - снес для этого уже имеющийся sql-server, и теперь ловлю траблы при pip install mysql-server. Пиздец))

Но проект идти должен, а потому я написал базовый вариант функций взаимодействия с БД. Комментирую каждую:

1. def make_connection(): в начале работы коннектимся к БД один единственный раз - все просто

2. def add_user(): вношу юзера в нужную таблицу (структура БД лежит в notion)
nickname вытащим, как аттрибут объекта сообщения юзеру боту (он лежит в аттрибуте .username или типа того
Нужно подумать над тем, в каком виде юзер будет вводить инфу и как ее предобработать. например, если он будет намеренно флудить или допускать ошибки/опечатки

3. def add_events(): все точно так же

4. get_data(): тут у меня 2 гипотезы:

	1) Из БД вытащить все, а потом уже промэтчить и отобрать (как я и сделал)
	2) К строкам БД применить функцию мэтчинга и отобрать с помощью SELECT ... ORDER BY 	*DISTANCE* LIMIT n

Также решил не делить ее пока на get_users и get_events, а просто поставил флажок, ибо функции идентичны. Так же нужно сделать и с add_user и add_event по сути, но тут я уже не стал "в слепую" пробовать

Надо: созвониться и с твоей умной головой поставить мне блядский mysql-server, если я не сделаю это сам
